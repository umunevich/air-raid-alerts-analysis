# Optional overrides for feature engineering (see configs/default.yaml for defaults).
# display_timezone is taken from project.timezone_display in default.yaml.

lags_hours:
  - 1
  - 3
  - 6
  - 24
  - 48
  - 168
